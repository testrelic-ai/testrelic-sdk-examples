# Appium mobile suite (optional — only needed if you have an Appium server +
# device/emulator). Without these the suite auto-skips.
#   pip install -r requirements-appium.txt

testrelic-appium>=0.1.0,<0.2
Appium-Python-Client>=4.0,<6.0
