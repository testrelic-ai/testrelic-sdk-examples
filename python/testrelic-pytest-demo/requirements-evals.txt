# DeepEval LLM-evaluation suite (optional, heavier install).
#   pip install -r requirements-evals.txt
#
# testrelic-deepeval is the LLM-eval package (import name `testrelic`), published
# on PyPI. The `[deepeval]` extra pulls in deepeval itself.

testrelic-deepeval[deepeval]>=0.1.2,<0.2
